# This build is UI-only (front-end flow demo for client review).
# No environment variables are required yet — Stripe, database, email,
# and storage keys will be added here once the back end is wired up.
